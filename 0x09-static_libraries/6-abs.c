#include "main.h"
/**
 *_abs - Program computes the absolute value of an integer
 *@n: the integer to chack
 *Return: The absolute value
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
return (-n);
}
